$(function() {
    
    set_modal_resend_invitation = function(){
        var selector = '.resend-email'
        var modal = '#modal-resend-invitation'
        attachClickLoadModal(selector, modal)
    }
    
    set_modal_resend_invitation()
    
});